type Book {
  author: String
  id: Int!
  name: String
}

type Mutation {
  addBook(bookInput: BookInput): Book
}

type Query {
  bookById(id: Int!): Book
}

input BookInput {
  author: String
  id: Int!
  name: String
}
